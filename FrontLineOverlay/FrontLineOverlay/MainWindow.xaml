<Window x:Class="FrontLineOverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FrontLine Lyrics" Height="160" Width="800"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True" 
        ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Grid>
        <Border x:Name="MainBorder" Background="#B3000000" CornerRadius="12" Padding="20">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock x:Name="LblAnterior" Foreground="#888888" FontSize="14" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock x:Name="LblAtual" Foreground="#e6e2c3" FontSize="26" FontWeight="Bold" TextWrapping="Wrap" TextAlignment="Center" Text="Waiting for connection..."/>
                <TextBlock x:Name="LblFutura" Foreground="#888888" FontSize="14" TextWrapping="Wrap" TextAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Thumb x:Name="ResizeGrip" Width="16" Height="16" HorizontalAlignment="Right" VerticalAlignment="Bottom" 
               Cursor="SizeNWSE" DragDelta="ResizeGrip_DragDelta" Visibility="Collapsed">
            <Thumb.Template>
                <ControlTemplate TargetType="Thumb">
                    <Grid Background="Transparent">
                        <Path Data="M 16,4 L 4,16 M 16,9 L 9,16 M 16,14 L 14,16" 
                              Stroke="#888888" StrokeThickness="2" Stretch="None"/>
                    </Grid>
                </ControlTemplate>
            </Thumb.Template>
        </Thumb>
    </Grid>
</Window>